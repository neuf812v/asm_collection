# Build artifacts
*.elf
*.map
*.o
*.lst

# Build-output binaries (regenerated by `make`).
# Reference binaries that are inputs (e.g. WS2812 bootloader/partition) are kept.
RISC-V/blink/blink.bin

# Claude Code local settings (machine-specific permissions)
.claude/settings.local.json
